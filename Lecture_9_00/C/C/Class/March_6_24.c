#include<stdio.h>

int  main()
{
    int a,b;
    printf("Enter two number:");
    scanf("%d %d",&a,&b);
    // printf("Sum of %d and %d is %d",b,a,a+b);
    printf("%d+%d=%d",a,b,a+b);
    return 0;
}